{
  // --------------------------------------------------------------------
  // 루트 tsconfig.json
  // - shadcn CLI가 여기서 alias 존재 여부를 검사하므로,
  //   반드시 이 파일에 baseUrl / paths를 명시해 둡니다.
  // - Vite + React-TS 기본 템플릿의 참조(tsconfig.app.json, tsconfig.node.json)는 그대로 유지합니다.
  // --------------------------------------------------------------------
  "compilerOptions": {
    // 상대 경로 해석의 기준 폴더를 프로젝트 루트로 지정합니다.
    // "@/..." 같은 별칭을 사용할 때 기준점이 됩니다.
    "baseUrl": ".",

    // 경로 별칭 정의
    // - "@/components/*"  → "src/components/*"
    // - "@/lib/*"        → "src/lib/*"
    // - "@/*"            → "src/*" (광범위한 별칭, 위 둘을 포함하는 상위 별칭)
    "paths": {
      "@/*": ["src/*"]
    }
  },

  // TypeScript가 소스 코드를 찾을 기본 포함 경로
  // - 솔루션 스타일(tsconfig.app.json, tsconfig.node.json)과 함께 사용해도 무방합니다.
  // - 편집기(IDE)에서 alias 인식을 확실히 하기 위해 추가합니다.
  "include": ["src"],

  // 기존의 프로젝트 참조는 그대로 유지합니다.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
